#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <stdint.h>

uint32_t division(uint32_t nombre, uint32_t diviseur);
int get_nb_bits(int nombre);
int division_binaire(uint8_t nombre, uint8_t diviseur);
